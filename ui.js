import { windChart, windChartCanvas, updateWindChart } from './windChart.js';
import { windHistory, timeRangeSec } from './main.js';

export function setupUI() {
  // 必要に応じてmain.jsからUI初期化・ボタン・CSV保存等の処理を移植してください
}
